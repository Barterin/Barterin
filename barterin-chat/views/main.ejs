<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="socket-url" content="<%= socketUrl %>" />
        <meta name="token" content="<%= token %>" />
        <meta name="api-url" content="<%= apiUrl %>" />
        <meta name="user-target" content="<%= idTarget %>" />
        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
        />
        <link
            rel="stylesheet"
            href="https://use.fontawesome.com/releases/v5.15.4/css/all.css"
        />
        <link rel="stylesheet" href="/style.css?<%= + new Date() %>" />
        <script defer src="/moment.min.js"></script>
        <script defer src="<%= socketUrl %>/socket.io/socket.io.js"></script>
        <script defer src="/plugins/jquery/jquery.min.js"></script>
        <script defer src="/script.js?<%= + new Date() %>"></script>
        <title>Chat</title>
    </head>
    <body>
        <div class="d-flex justify-content-between">
            <div class="active vh-100 pt-3 pb-3" id="chat-list"></div>
            <div
                class="d-flex flex-column justify-content-between active"
                id="chat-content"
            >
                <div
                    class="chat-content-header d-flex align-items-center px-3 py-2"
                >
                    <i class="fas fa-arrow-left me-3" id="chat-btn-back"></i>
                    <img
                        id="user-image"
                        class="chat-content-picture img img-fluid rounded-circle"
                        src="http://localhost:8000/uploads/images/profiles/default.png"
                    />
                    <div class="summary w-100 ms-3">
                        <p class="name p-0 m-0" id="name"></p>
                        <p class="option p-0 m-0" id="option"></p>
                    </div>
                </div>
                <div class="p-3" id="chat-content-body"></div>
                <div
                    class="d-flex align-content-center ps-3 pe-2 py-3"
                    id="chat-content-footer"
                >
                    <textarea
                        class="form-control"
                        id="message-input"
                    ></textarea>
                    <button class="btn">
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
            </div>
        </div>
    </body>
</html>
